// Functions & types

function add(a: number, b: number) {
  return a + b;
}

// it includes output type and also return type

function print(value: any) {
    console.log(value);
//   return value;
}

// there is no return statement.
// so there is a special return type called void.
// void is basically comparable to null and undefined.
// it means this function never return
